h1 Listing details

table
  thead
    tr
      th Person
      th
      th
      th

  tbody
    - @details.each do |detail|
      tr
        td = detail.person
        td = link_to "Show", detail
        td = link_to "Edit", edit_detail_path(detail)
        td = link_to "Destroy", detail, data: {confirm: "Are you sure?"}, method: :delete

br

= link_to "New Detail", new_detail_path
